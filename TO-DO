    ***коннект к бд
        ***записывать данные бд в начало файла(хранить все) loadSettingsFromFile
        ***(Jcombobox для вариантов)
        ***если настр были считына с файла, их не записывать
        ***(приделать логгер)
        ***почти готов: разобраться с файлстримами,
        ***привести в порядок код

при коннекте к бд загружать бд , ее таблицы и атрибуты
    возможность изменятьь и сохранять имена атрибутов на клиентской стороне

у каждой видимой таблицы есть окно для запроса и для вставки
    они могут состоять из:
        выбор даты
        выбор значения из списка
        ввод значение в поле
при выборе таблицы из списка:
    сетать ее ссылку в поле, к которому будут обращаться кнопки 'добавить' и 'запросить'
сделать загрузщик загрузочной информации

добавить и другие таблицы, абстракция

добавть типы :
	проезд, услуги, продукты питания
